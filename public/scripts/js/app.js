// Generated by CoffeeScript 1.6.3
(function() {
  define(["marionette", "main_router", "views/main_layout"], function(Marionette, MainRouter, MainLayout) {
    var App;
    App = new Marionette.Application();
    App.routers = {
      mainRouter: new MainRouter()
    };
    App.addInitializer(function(options) {
      console.log("App is initializing", options, this);
      return App.vent.on('huzzah', function() {
        return console.log("Huzzah!");
      });
    });
    App.on("initialize:before", function(options) {
      return console.log("About to run the App's initializers", options, this);
    });
    App.on("initialize:after", function(options) {
      console.log("Just finished running the App's initializers", options, this);
      return Backbone.history.start({
        pushState: true
      });
    });
    App.on("start", function(options) {
      console.log("The app has started", options, this);
      App.vent.trigger("huzzah");
      return App.container.show(new MainLayout);
    });
    App.addRegions({
      container: "#container"
    });
    return App;
  });

}).call(this);
